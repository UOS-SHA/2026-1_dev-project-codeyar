# 🖥️ 코드야르 (CodeYar) — 프로젝트 계획서

> **작성일**: 2026-04-02  
> **버전**: v1.0

---

## 1. 프로젝트 개요

| 항목 | 내용 |
| :--- | :--- |
| **팀명** | 코드야르 (CodeYar) |
| **팀원 수** | 6명 |
| **PM** | 신승리 |
| **한줄 소개** | 코딩 시험용 온라인 저지 |
| **프로젝트 성격** | 학습용 데모. 실습실에서 시연 가능한 수준을 목표로 함. |

---

## 2. 프로젝트 주제

코딩 시험 때 **부정행위를 방지**하기 위한 기능과, 제출한 **코드의 악성 행위를 방지**할 수 있는 기능을 가진 온라인 저지 시스템.

---

## 3. 운영 전제

| 항목 | 결정 |
| :--- | :--- |
| **학생 PC (클라이언트)** | Windows 전용 |
| **서버 + 채점기** | Linux 가능 (Docker 활용) |
| **사용 환경** | 감독관이 있는 **실습실** (원격 시험 ❌) |
| **팀 규모** | 6명 |
| **채점 언어** | Python (향후 C, Java 확장 검토) |

---

## 4. 기술 스택

| 구성 요소 | 기술 |
| :--- | :--- |
| **클라이언트** | C# (Windows Forms) |
| **프론트엔드** | TypeScript (React) |
| **백엔드** | Python (FastAPI) |
| **채점 엔진** | **Judge0** (Self-hosted, Docker) |
| **DB** | TBD |

---

## 5. 시스템 구성 (큰 그림)

시스템은 크게 3개의 덩어리로 구성됩니다.

| 덩어리 | 하는 일 |
| :--- | :--- |
| ① **시험 클라이언트** (C# WinForms) | 학생용 Windows 앱. 전체화면 강제 + 코드 에디터. 부정행위 이벤트 감지 및 전송. |
| ② **서버** (FastAPI) | 로그인, 시험/문제 관리, 제출 저장, 결과 전달, 이벤트 로그 수집. |
| ③ **채점기** (Judge0) | 학생 코드를 격리 환경에서 실행하고 채점. |

**흐름**: ①에서 코드 작성 → ②로 전송 → *[AST 검사]* → ③에서 실행/채점 → 결과를 ①에 표시

---

## 6. 핵심 기능

### 6-1. 교수 기능
- 문제 출제 (제목, 설명, 입/출력 데이터셋, 조건 등)
- 시험 시작/종료 관리
- 학생 성적 확인 및 통계
- **부정행위 리포트 확인** (이벤트 로그 기반)

### 6-2. 학생 기능
- 코드 에디터에서 코드 작성
- 시험 시작 전까지 대기
- 코드 제출 및 채점 결과 확인

### 6-3. 안티치트 전략

두 가지 축: **억제(Prevent)**와 **탐지(Detect)**

#### 🚫 억제 (가능한 범위에서 제한)
- 화면 전환 (Alt-Tab 등) → 전체화면 강제 + 가능한 범위에서 제한 시도
- 키보드 단축키 → 주요 단축키 제한 시도 (WinForms + OS 정책 조합)
- 자동완성/코드 제안 → 코드 에디터에서 비활성화

#### 👁️ 탐지 + 로깅 (발생 시 기록)
- 붙여넣기 이벤트 → 발생 여부 기록 + 정책에 따라 내용도 기록
- 앱 포커스 이탈, 재시작, 네트워크 끊김 등 → 이벤트 로그 전송
- 로그를 바탕으로 교수가 의심 행위를 검토 가능

### 6-4. 코드 실행 보안 (Judge0 + Docker)
- **무한루프** → 실행 시간 제한 초과 시 강제 종료
- **메모리 폭주** → 메모리 사용량 제한 초과 시 강제 종료
- **위험한 실행** → Docker 컨테이너 격리, 네트워크/파일시스템 접근 차단
- **위험 호출 탐지** → `os.system()`, `subprocess` 등 금지 호출 탐지

### 6-5. 코드 구조 검증 (AST 분석, Python 한정)
- 교수가 출제 시 "이 함수를 구현해라", "재귀로 풀어라" 같은 조건 설정
- Python `ast` 모듈로 제출 코드를 파싱하여 조건 충족 여부를 검토
- 예: "fibo 함수 존재?", "자기 자신 호출?"

---

## 7. 단계별 구현 계획 (MVP → 확장)

| 수준 | 억제 | 탐지/로깅 | 코드 보안 |
| :--- | :--- | :--- | :--- |
| **MVP** | 전체화면 강제 + 이탈 시 복귀 유도 | 붙여넣기 로깅 + 이탈 기록 | 시간/메모리 제한 + AST 검토 |
| **확장 1** | 단축키 제한 시도 | 의심 행위 리포트 | 금지 호출 탐지 |
| **확장 2** | 프로세스 감시 시도 | 실행 프로그램 기록 | — |

---

## 8. 비고 및 제약 사항
- 원격 시험 지원 ❌ (실습실 내 사용 전제)
- 완전한 커닝 방지는 목표가 아니며, **억제 + 탐지 로그를 통한 감독 보조**를 목표로 함.
- C# 클라이언트에서는 OS 수준 제한에 한계가 있어, 교수/감독관의 물리적 감독과 병행을 권장.
